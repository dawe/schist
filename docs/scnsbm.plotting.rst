***********************
scnsbm.plotting package
***********************

scnsbm.plotting._alluvial
#########################

::

    def alluvial(adata: AnnData, level_start: 1, level_end: None, key: 'nsbm', gap: 0.05) 

Generate an alluvial plot for NSBM hierarchy. This plot may be useful to study the hierarchical relations in a Nested Model generated by ``scnbsm.inference.nested_model`` .

Parameters
**********
    *   **adata**: ``AnnData``
            The annotated data matrix.
    *   **level_start**: ``int`` (default: ``1`` )
            The deepest level in the hierarchy (i.e. the one with more clusters). Note that this parameter marks the leafs of the tree.
    *   **level_end**: ``int`` (default: ``None`` )
            The coarser level in hierarchy. Default value is `None`, if pruning has not been performed, it may be useful to exclude uninformative levels anyway. Note that this parameter marks the root of the tree.
    *   **key**: ``str`` (default: ``'nsbm'`` )
            The key used to group cells. Default is `nsbm`.
    *   **gap**: ``float`` (default: ``0.05`` )
            The gap between groups in percentage.
            

.. automodule:: scnsbm.plotting
   :members:
   :undoc-members:
   :show-inheritance:
